package de.htw.fp.mitschriften

/*
object seminar6 extends App {
  def main(args: Array[String]) {
    // def zZ_fold (s: List[Char]):
    //val enoty: MAp[Char],Int

    //foldLeft(s, empty(s, empty, (stat: Map[Char, Int]), c: Char)))

    /**
     * reduce und fold sind durch Prozessor parallelisierbar (nicht fold left oder fold right)
     *
     */
    val l = List(1, 2, 3)
    val lp = l.par
    // List -> parallelisierbare Liste

    //def produkt (seq: Seq  [Double]) =     
    // mapreduce (seq, x => x, (x,y) => x*y)

    // def summe ..
    //mapreduce()seq, _ , _+x)

    // def quadratsumme} ..
    // mapreduce (seq, x => x*x, _+_)

    // def fakultät (n: Int)
    // mapreduce (1 to n, _ , _ * _)

    // def fakultät2 .. = produkt(1 to n)

    val xl = List(1, 2, 3)
    val yl = List(4, 5, 6)
    val pl = xl zip yl // => List((1,4),(2,5), (3,6))

    // aus liste personennamen filtern welche über oder 18 jahre sind

    // for (p <- personen if p.alter >= 18) yield p.name

  }
}
*/