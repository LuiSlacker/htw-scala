package de.htw.fp.assignment2

class assertions {

  //def runAssertions(coinOptions: List[Any] => List[(Any, Int)]) = {
   // coinOptions(List())
  //}
  def runAssertions(countCharFrequencies: (Int, List[Int], Int) => Int) = {
    // countCharFrequencies()

  }
}